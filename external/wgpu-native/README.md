# wgpu-native

Prebuilt release binaries of [wgpu-native](https://github.com/gfx-rs/wgpu-native)
v29.0.1.1 (shared libraries only) plus the standard `webgpu.h` / `wgpu.h`
headers. `lib/<platform>` is selected by the top-level CMakeLists.txt.
